<nav class="navbar is-relative is-transparent py-3" aria-label="main navigation">
    <div class="container">
        <div class="navbar-brand">
            <a class="navbar-item" href="{{ path('homepage') }}">
                <img src="{{ asset('icons/logo-2.svg') }}" alt="" width="auto" style="min-height: 50px; max-height: 5em">
            </a>
            <a class="navbar-burger" role="button" type="button" data-toggle="side-menu" data-target="#sideMenuHeaders06" aria-controls="sideMenuHeaders06" aria-expanded="false" aria-label="Toggle navigation">
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
            </a>
        </div>
        <div class="navbar-menu">
            <div class="navbar-start">
                <a class="navbar-item" href="{{ path('app_contact') }}">Contact</a>
                <a class="navbar-item" href="#">Company</a>
                <a class="navbar-item" href="#">About Us</a>
                <a class="navbar-item" href="#">Features</a>
            </div>
            <div class="navbar-end">
                <div class="navbar-item">
                    <div class="buttons">
                        {% if app.user %}
                        <a class="button is-primary" href="{{ path('app_logout') }}">Déconnexion</a>
                        {% else %}
                        <a class="button is-primary" href="{{ path('app_login') }}">Connexion</a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>